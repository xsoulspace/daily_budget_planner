# packages/yjs_server/pubspec.yaml
name: collaborative_todo_server
version: 0.1.0

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  logging: ^1.3.0 # Added for better logging

dev_dependencies:
  test: ^1.31.2
